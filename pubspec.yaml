name: oauth_json_place_holder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  flutter_modular: ^6.4.1
  dio: ^5.9.0
  fpdart: ^1.1.1
  equatable: ^2.0.7
  pretty_dio_logger: ^1.4.0
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
